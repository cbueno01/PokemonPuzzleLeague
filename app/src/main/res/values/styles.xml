<resources>
    <style name="MenuActivityTheme" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:textColorPrimary">@color/primary_text_color</item>
        <item name="android:textColorSecondary">@color/primary_text_color</item>
        <item name="actionMenuTextColor">@color/primary_text_color</item>
        <item name="android:navigationBarColor">@color/primary_dark</item>
    </style>

    <style name="GameTheme" parent="NoTitleOrActionBar">>
        <item name="android:windowBackground">@color/primary_dark</item>
        <item name="android:colorBackground">@color/primary_dark</item>
        <item name="android:alertDialogTheme">@style/AlertDialogTheme</item>
    </style>

    <style name="SettingsTheme" parent="FullScreen">
        <item name="android:navigationBarColor">@color/primary_dark</item>
        <item name="android:actionBarStyle">@style/ActionBarStyle</item>
        <item name="android:alertDialogTheme">@style/AlertDialogTheme</item>
    </style>

    <style name="NoTitleOrActionBar" parent="FullScreen">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="FullScreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="ActionBarStyle" parent="@android:style/Widget.ActionBar">
        <item name="android:logo">@android:color/transparent</item>
        <item name="android:background">@color/primary_dark</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/primary_text_color</item>
        <!-- Used for the title and text -->
        <item name="android:textStyle">bold</item>
        <item name="android:textColorPrimary">@color/primary_text_color</item>
        <!-- Used for the background -->
        <item name="android:background">@color/primary_dark</item>
        <item name="colorControlNormal">@color/primary_text_color</item>
    </style>
</resources>

